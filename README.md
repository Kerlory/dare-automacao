# Projeto.Dare

Este é um projeto Java simples com o pacote `com.dare`.

## Como compilar (sem build tool)

No Windows PowerShell:

```powershell
.\build.bat
```

## Como executar

```powershell
.\run.bat
```

## Usando Gradle (se instalado)

```powershell
gradle run
```
